global:
  scrape_interval: 10s # Як часто збирати метрики

scrape_configs:
  - job_name: 'doc-classifier-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # Звертаємося до бекенду за його ім'ям у Docker-мережі
      - targets: ['doc-classifier-backend:8080']